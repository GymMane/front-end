export interface userInfo {
  // LoginID: number;
  // EmailID: string;
  // MobileNumber: string;
  // LoginName: string;
  // Password: string;
  // RoleID: number;
  // Deleted: boolean;
  // LastLoginDateTime: string;
  // Role: string;
  access_token: string;
  token_type: string;
  expires_in: number
  userInfo: string;
}
